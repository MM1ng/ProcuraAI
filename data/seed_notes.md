# Seed Notes

`products.csv` is generated by `python -m app.scripts.generate_products`.
The generator creates deterministic enterprise office procurement products with
realistic brands, prices, ratings, stock levels, suppliers, delivery days,
warranty periods, compliance labels, descriptions, and tags.

The local demo can run fully from CSV/JSON fallback files. SQLite seeding is
available for database-backed inspection and future extension.
